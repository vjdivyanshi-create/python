# PySpark Partition Demo

## Objective
Create a DataFrame with 5 million records using spark.range().
Display the initial number of partitions.
Increase partitions to 12 using repartition().
Reduce partitions to 3 using coalesce().

## Requirements
- Python
- PySpark

## Run

```bash
python partition_demo.py